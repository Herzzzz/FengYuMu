# 枫语幕 v2.0.2

- 修复聊天框出现新消息却偶尔不翻译、积累多条后才突然翻译的问题。
- 截图识别和 AI 翻译解耦：模型翻译时仍持续检查聊天框，新消息按顺序排队。
- 以相邻聊天画面判断新增消息，支持聊天框滚动及连续重复消息。
- 轮询间隔由 1 秒缩短到 650 毫秒。
- 新增 50 余条英文缩写、网络俚语和冒险岛聊天术语，包括 `rn`、`idk`、`ngl`、`brb`、`cc`、`leech`、`mule`、`proc`、`cooked` 等。
- 缩写按完整单词匹配，不会误伤普通英文单词。
- 延续 v2.0.1 的玩家名保护、聊天上下文、英文照抄重试和 `posture check` OCR 纠错。

已有模型无需重新下载。替换 `枫语幕.exe` 与 `枫语幕词库.tsv`，再点击一次“同步AI词库”即可。
