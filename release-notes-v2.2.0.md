# 枫语幕 v2.2.0

本次更新以 v2.1.2 为准确度基线，重点改善启动稳定性、异常隔离和主界面易用性。

- 启动后显示简洁主界面；词库完成内存载入后显示就绪，再缩到托盘返回游戏。
- AI 知识改为打开 AI 功能时按需初始化，模型仍只在第一次真正翻译时加载；AI 初始化或联网失败不会中断 F8 本地词库翻译。
- 增加轻量安全预热与词库旧索引回退；新索引完整加载成功后才替换，失败继续使用上一次稳定索引。
- 主界面只保留“缩到托盘，开始使用”、词库、快捷键和 AI 聊天翻译，关闭窗口只缩到托盘。
- EXE、主窗口和托盘统一采用游戏内 ID 1012370 的原版透明枫叶像素图标。
- 基准模式增加截图、主 OCR、详情复核、面板复核、兜底复核和合并绘制分阶段计时，没有为提速删减 OCR 路径。
- 完整回归覆盖装备、物品 Tooltip、技能、任务、角色属性、长短句、动态数字、同名词条和聊天框排除；五张实图全部低于 2 秒。

安全边界不变：只使用外部窗口截图、Windows OCR、全局快捷键和独立覆盖窗；不读写游戏内存、不注入、不模拟键鼠。

已有离线 AI 模型和配置无需重新下载或重设。更新时替换 EXE、词库和说明即可。
