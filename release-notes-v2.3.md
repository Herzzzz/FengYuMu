# 枫语幕 v2.3

- 新增“范围最大 / 均衡 / 范围最小”三档滑动条，设置立即生效并自动保存。
- 动态优先处理：详情框 > 对话框内 > 对话框外 > 当前界面；缺少前一类时后续区域自动前移。
- 范围最大完整保留 v2.2.2 识别路径；均衡最多复核 3 个区域，范围最小最多复核 2 个区域。
- 修复技能等级右括号被误读成数字 1、NPC 完整句与句内短词重复覆盖、NPC 对话误触发任务窗口模板。
- 新增魔法师转职 NPC 两句完整对白及常见问号 OCR 变体。
- 新旧同机对照：4 张新增公开截图、23 个人工核对关键字段，v2.2.2 为 18/23，v2.3 三档均为 23/23；旧版平均 863ms，v2.3 最大/均衡/最小分别为 862/922/922ms。该准确率只代表本组样本。
- 原有 5 张用户实测图继续全部通过（1121–1709ms）；新增 4 张公开图三档共 12 组全部低于 1.5 秒。
- 修复 Windows 任务栏重建后进程仍在、托盘图标却消失的问题；再次双击 EXE 会唤回已有实例并重建托盘图标，不会重复驻留。
- 安全边界不变：只截取窗口画面并使用 Windows OCR 和独立覆盖窗，不读写游戏内存、不注入、不修改游戏文件、不模拟键鼠。

公开测试图来自 MapleRoyals 社区页面，仅用于回归测试，不包含在发布包中。
