# 枫语幕 v2.0.3

- 修复聊天框没有新消息时，OCR 少量字符抖动导致同一内容反复翻译。
- 保留玩家真正连续发送相同消息的能力，不会被稳定去重误吞。
- `[Notice]` 系统公告独立识别，不再把 `private`、`Witnprivate` 等正文误当玩家名。
- 改进频道徽标、不同玩家消息和换行公告的拆分。
- 新增 `DK`、`ppl`、`pointy sticks`、`meso sack` 等冒险岛聊天语境。
- 优先完整使用 Vulkan 显卡层，并适度提高 CPU 线程和批处理大小；不支持时自动降级。

模型文件和格式没有变化。已经安装模型的用户只需替换 `枫语幕.exe` 与 `枫语幕词库.tsv`，再点击一次“同步AI词库”，无需重新下载约 2.5GB 的模型。
