# 枫语幕 v2.2 开发版

本次更新以 v2.1.2 为冻结准确度基线，优先处理启动稳定性、异常隔离和主界面易用性。

- 启动后显示主界面，不再直接缩进托盘；词库载入内存后保持待命，仍只在按下呼出快捷键时截图和 OCR。
- AI 知识改为打开 AI 功能时按需同步。AI 初始化、模型或联网失败不会中断 F8 本地词库翻译。
- 增加安全预热与旧索引回退；预热失败时继续使用上一次的稳定索引。
- 主界面缩小，删除与 F9 重复的“隐藏当前翻译”按钮，保留“缩到托盘，开始使用”、词库、快捷键和 AI 聊天翻译。
- EXE、窗口和托盘统一采用游戏内 ID 1012370 的原版透明枫叶像素图标。
- 基准模式增加各阶段耗时记录。五张真实截图显示，屏幕截图仅约几十毫秒，主要耗时来自 OCR 与面板复核；为避免少翻、错翻和长句碎片化，本版没有删减既有识别路径。
- 回归基线覆盖装备/物品 Tooltip、技能、任务、角色属性、长短句、动态数字、同名词条和聊天框排除。

安全边界不变：只使用外部窗口截图、Windows OCR、全局快捷键和独立覆盖窗；不读写游戏内存、不注入、不模拟键鼠。
