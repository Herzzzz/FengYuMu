# 枫语幕 v3.0

- 按“详情框 > 对话框内 > 对话框外 > 当前界面”执行场景优先级；没有可靠面板时才处理当前界面，减少全屏零碎短词。
- 人物信息与人物属性改用独立结构判断，修复角色信息偶发套用属性布局、职业名被误判为武器类型，以及混合窗口标签重复的问题。
- 点击“开始实时翻译”会直接打开 AI 独立浮窗，不再要求额外勾选。浮窗采用接近游戏聊天框的半透明无边框样式，四边/四角可自由调整形状，自动记忆位置大小，置顶但不抢游戏焦点，也不接管 F8 截图覆盖。
- 关闭 AI 实时翻译主窗口现在只隐藏控制界面，不再停止 140ms 监听或连带隐藏独立浮窗；只有主动点击“停止实时翻译”才停止监听。
- 独立浮窗自己的红叉改为“只隐藏”：后台继续翻译，新消息不会把窗口强行弹回；托盘右键新增“重新显示AI翻译悬浮窗”，托盘双击仍只打开主界面。
- 联网 AI 从“本地机翻后复核”改为直接主翻译：默认推荐豆包 Seed 2.0 Lite，提供 DeepSeek V4 Flash 快速备选、智谱 GLM-4-Flash 免费备用和自定义兼容接口。设置页直接显示接口、模型、Key 和小白教程，不再藏进高级项；每句都会注入冒险岛怀旧服语境与命中词库，只输出最终译文，失败时自动切回离线备用。
- 主界面新增“一键更新”：直接下载 GitHub 最新正式 ZIP，先校验 ZIP 的 SHA-256，再逐个校验三个待替换文件；完整备份当前版本后才重启替换，任一步失败都会保留或自动恢复更新前版本。同为 v3.0 但发布包已替换时也能按文件哈希识别。
- AI 浮窗从当前游戏画面采样每条消息的字体色，不把普通、好友、组队、家族、联盟、悄悄话、系统和公告强行归并成固定色；浅蓝整行底色按普通喇叭显示，浅粉整行底色按超级喇叭显示。
- AI 聊天改为按 OCR 物理行解析并加入相邻三帧防抖；修复装备属性文字混入、总文本粘连、`SHOT` 被误当频道、好友上线人名被截断，以及一次 OCR 漏行后旧消息重复出现。真实连续发送的同文消息仍会保留。
- AI 聊天监听缩短到 140ms；聊天区 OCR 在 22 张问题图/跨视频实帧中保留胜出的 1800 紧裁档，1900/2050 放大退化档未采用。联网就绪时跳过本地预热，只有使用离线备用时才提前加载并防止重复启动；本机 8B 模型 12 条跨场景实测冷启动 4,425ms，热态最慢 333ms。新增玩家口语、交易、活动、组队和刷材料意图规则，常见句式无需等待模型；繁体输出和提示词泄漏会被拦截。
- 修复持续扫描占用识别状态时 F8 看似无反应：手动 F8 现在抢占自动扫描，并在截图前临时隐藏 AI 控制窗和浮窗；快捷键注册冲突会显示失败并恢复原绑定，不再静默保存无效组合。
- 首次使用 F8 或手柄呼出时根据底部玩家聊天行锁定聊天框，普通截屏识别排除该区，AI 聊天翻译复用同一区域；用户手动框选始终优先。
- 修复悬浮层 `Graphics.MeasureString` 参数异常导致全屏白底红叉：绘制前检查文本和矩形，逐标签隔离 GDI+ 错误，关闭过程中不再使用已释放字体。
- 新增场景化识别层：人物属性、技能、任务、NPC 对话、装备/物品详情、商店和背包分别积累可靠证据，再进入专用 OCR 与词库匹配；低置信度时不猜译。
- 新增默认关闭的“持续自动翻译”：打开游戏面板后自动显示，无需按 F8；无面板先做低成本探测并智能退避，不重复运行昂贵高分辨率 OCR。F8/F9 和 v2.3“范围最大”兼容路径完整保留。
- 低分辨率经典人物属性面板改为同时定位洋红字段列与蓝色属性列；高分辨率历史人物图继续通过。
- 装备结构化数值增加通用 OCR 纠错：`2S→25`、`SO→50`、`O→0`，仅限等级、需求和属性数字字段，避免污染普通文字。
- 收录视频 `5420ff115e9d68f83d6826d48dfe1cec.mp4` 中可人工确认的 35 条任务标题/摘要/对白；保留原词库前缀，模糊或没有显示完整内容的项目不猜写。
- 公开实机回归：7 类高价值场景三档共 21/21 通过；持续模式 7/7 通过；23 个视频任务面板全部低于 2 秒。低价值背包图不计入命中率。
- v2.3/v3.0 同图 A/B：35 个人工关键项从 13/35 提升到 35/35，本轮平均耗时 664ms 与 717ms；精度提升没有删除 v2.3“范围最大”兼容路径。
- 用户后来补发的 21 张问题图已逐张重跑，全部在 1.6 秒内完成；好友列表保持零覆盖，完整人物属性面板稳定命中 23 个字段。零命中的非目标界面不计为命中率。
- 资源实测（Windows 11、i5-12490F、32GB）：无面板探测平均 70ms、最大 114ms；最重技能面板持续翻译平均 1,437ms、最大 1,532ms，峰值私有内存 227.3MB。
- 安全边界不变：只使用前台窗口截图、Windows OCR、全局快捷键和独立透明覆盖层；不读写游戏内存、不注入、不修改游戏文件、不模拟键鼠。
- 最新视频词库 35 条与本轮 32 条高置信玩家术语均已纳入；完整词库 SHA-256：`008004ABEF8C34D61C00E5C785F76A33CCAC00075D0CCDBF2E942926B33ECBB3`。

离线 AI 模型安装后不需要账号、API 或联网。联网 AI 不需要租服务器，但豆包、DeepSeek、智谱等第三方 API 仍要求玩家自己的 API Key；Key 仅用 Windows 当前账户加密保存在本机，本版不内置公共密钥。连接测试会直接说明密钥错误、模型未开通、额度不足或网络异常，不再只显示笼统失败。

正式支持的最低配置：Windows 10/11 64 位、英语 OCR、四核处理器、16GB 内存、独立显卡、SSD、1280×720。推荐六核及以上处理器、32GB 内存和支持 Vulkan 的独立显卡。CPU 回退仅作为启动失败时的安全兜底，不属于正式支持配置。
