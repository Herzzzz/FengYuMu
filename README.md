# 枫语幕

枫语幕是一个面向 Windows 的《冒险岛国际怀旧服》屏幕翻译覆盖工具。

程序启动后把词库载入内存并在托盘后台等待。按下呼出快捷键时，它优先只截取当前前台窗口的客户区，对画面放大并增强文字对比度，再使用英语 Windows OCR 识别原文，只把词库命中的内容用中文覆盖到原位置；按下缩回快捷键后立即撤下覆盖层。

## 使用

1. 从 Releases 下载最新版压缩包。
2. 保持 `枫语幕.exe` 与 `枫语幕词库.tsv` 在同一目录。
3. 双击 `枫语幕.exe`。程序默认进入托盘后台。
4. 默认 `F8` 呼出翻译，`F9` 缩回后台。
5. 按 F8 前先点击游戏窗口，让冒险岛保持在最前面；程序不会翻译被其他窗口遮挡的内容。

每次按 F8 都会重新截取当前画面、重新识别并重新匹配。F9 会立即清空本次覆盖；再次按 F8 不会复用任何历史截图或历史翻译结果，因此角色移动、装备悬浮框改变后不会拿旧位置覆盖。

托盘右键只有三个功能：打开并更改词库、更改快捷键、退出。

## v1.2 任务词库

- 词库编辑窗口分成“常规词库”和“任务词库”两个标签，但仍只保存为一个轻量 TSV 文件。
- 任务列表按“任务代码＋英文任务名＋中文任务名”索引；双击任务即可单独编辑任务名、完整说明、短语或整句对白。
- 当前任务名补齐至 174 个，包含四个职业共 16 个当前转职任务环节。
- 截图中先确认存在任务窗口、任务标题或已收录的任务整句，才启用任务词库；普通装备、怪物、道具和技能窗口不会调用任务句子。
- 确认任务后优先在同一任务代码内匹配最长整句，避免把对白拆成多个一词多义的普通词条。
- 资料站不能可靠一一对应的对白没有自动硬配。玩家可在任务编辑页补充或修正，保存后立即重新载入内存。

## 识别改进

- 只截取按下快捷键时的前台窗口客户区，避免同时翻译旁边的聊天软件。
- 识别前自动把小字号画面放大到合适尺寸，并适度增强文字对比度。
- 国际服固定优先使用英语 OCR；系统没有英语 OCR 时才回退到系统语言。
- OCR 坐标会按放大比例换算回原窗口位置，中文覆盖仍对齐原文。
- 在一秒预算内自适应执行彩色增强、灰度高对比和更高分辨率三种 OCR；合并各轮独有的可靠词库命中，较慢电脑会自动减少轮数。
- 2,828 个物品、装备、技能、怪物和 NPC 的真实资料站图标已压缩为 64 位指纹，随文字词库一次性载入内存。
- 文字完全命中时直接翻译；文字有少量 OCR 错误时，只检查文字左侧附近的小图标并与候选文字共同确认，避免全屏遍历图标。

建议使用窗口化或无边框窗口。独占全屏可能不显示桌面覆盖层。

## 词库 1.2

词库 1.2 专门针对国际怀旧服当前内容整理，共 3,394 条（2026-08-12 数据快照）。常规名称沿用 1.1 的 3,379 条，另补齐 9 个任务名并加入 6 条可可靠对应的完整任务说明：

- 道具：2,525
- 地图：302
- 任务：174
- NPC：150
- 技能：81
- 怪物：72
- 常见界面：58
- 职业：15
- 地区：11

资料按同一游戏数据 ID 对齐英文名和简体中文名。明显无关的现代服内容、GM专用物品、无英文名以及同英文对应多个中文且无法从屏幕区分的歧义项不会进入自动覆盖词库。

## 词库格式

`枫语幕词库.tsv` 是 UTF-8 TSV 文本：

```text
英文原文<Tab>中文翻译<Tab>分类#资料ID<Tab>可选的真实图标64位指纹
```

示例：

```text
Snail	蜗牛	怀旧服-怪物#2
```

程序启动时一次性把常规索引、按任务代码隔离的任务索引和图标指纹载入内存，待机期间不反复读取硬盘，也不联网。修改后可在词库窗口点击“保存并载入内存”，无需重启。

## 贡献翻译

请阅读 [CONTRIBUTING.md](CONTRIBUTING.md)。提交时尽量保留第三列的资料 ID，以便核验。

## 数据来源与版权

词库名称对照主要参考：

- [冒险岛国际怀旧服 GCW 小册子](https://mxdgcw.dvg.cn/)：当前 GCW 内容范围、英文名、简体中文名与资料 ID。
- [NiaMeowDB MapleStory Classic](https://meowdb.com/msclassic/)：按 NPC ID 补齐部分英文名称。
- [冒险岛小册子任务库](https://mxd.dvg.cn/quests.php)：按任务代码复核中文任务名与任务文本结构。

本项目不包含游戏客户端、图片、音频或其他游戏资源。MapleStory、相关名称及游戏内容版权归 NEXON 及其关联方所有。

## 安全边界

程序不读取或修改游戏内存，不注入或挂钩游戏进程，不修改游戏文件，也不模拟鼠标键盘输入。它只使用前台窗口截图、英语系统 OCR、全局快捷键和独立透明窗口。

任何第三方防作弊系统都可能误报，本项目无法承诺绝对不会误报。使用前请自行确认所在服务器规则。

## 构建

在 Windows PowerShell 中运行：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\src\build.ps1
```

编译依赖 Windows 自带的 .NET Framework C# 编译器和 Windows OCR 组件。
